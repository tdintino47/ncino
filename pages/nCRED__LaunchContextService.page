<apex:page controller="nCRED.LaunchContextService"
  readOnly="false"
  action="{!performDml}"
  cache="false"
  contentType="application/json"
>{!pageResponse}</apex:page>  <!-- NOPMD - CSRF is protected against in the controller -->